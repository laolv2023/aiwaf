orjson>=3.9.0
redis>=5.0.0
aiokafka>=0.8.0
cachetools>=5.0.0
prometheus-client>=0.17.0
joblib>=1.3.0

# V6.0 出站适配器依赖（AIWAF 接入 Akto）
# kafka-python: V6.0 适配器使用同步 Kafka 客户端（与设计文档一致）
kafka-python>=2.0.0
# protobuf: Akto Protobuf 消息序列化/反序列化
protobuf>=4.25.0

# 测试
pytest>=7.0.0
pytest-asyncio>=0.21.0

# AI/ML (optional — anomaly detection with IsolationForest)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0
